{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/build/rt_shader_compiler",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}